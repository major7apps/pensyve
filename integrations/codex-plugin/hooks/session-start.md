Pensyve memory is available through the bundled MCP server when it is configured and authenticated. For substantive project work, detect the primary entity, run a brief `pensyve_recall` before making memory-sensitive claims, and keep any recall surface concise. If no Pensyve MCP tools are available, continue normally and mention the missing MCP connection only when the user asks for memory behavior.
